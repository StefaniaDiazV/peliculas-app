import { NavBar } from "../Navbar"

const Header = () => {
    return (
        <header style={{zIndex: '5'}} className="main-header">
            <NavBar/>
        </header>
    )
}

export { Header }